apiVersion: v1
kind: Service
metadata:
  name: genesis
  namespace: {{ .Release.Namespace }}
  {{- if .Values.warp.enabled }}
  labels:
    internal-cks.conductor.technology/warp: "true"
  annotations:
    internal-cks.conductor.technology/warp-hostname: {{ .Values.warp.hostname | quote }}
  {{- end }}
spec:
  selector:
    app: genesis
  ports:
    {{- if .Values.tls.enabled }}
    - port: 443
      targetPort: 443
      protocol: TCP
      name: https
    {{- end }}
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: frontend
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: backend
